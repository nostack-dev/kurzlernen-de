name: Restore photoreal 3D world once

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Photoreal restore retired: GOOGLE_MAP_TILES_BROWSER_KEY is not configured."
