name: Repair FPS player-camera transform once
on:
  push:
    branches: [main]
    paths: ['.github/workflows/repair_fps_player_camera_transform_once.yml']
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Always present weapon from player camera
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          p=Path('sim/first_person_weapon_runtime_v3.mjs')
          s=p.read_text()
          old='renderHook=function(...args){previousSceneOnBeforeRender?.apply(this,args);const mainCamera=bridge()?.threeCamera,camera=args[2]||mainCamera;if(!mainCamera||camera!==mainCamera)return;const gun=scene.getObjectByName?.("WALK_PISTOL_3D");if(!gun)return;const mode=String(footWeapons()?.mode||"pistol");setWeaponModeVisual(gun,mode);applyLatchedAimTransform(gun,mainCamera,performance.now());const view=viewport();if(view)view.dataset.walkWeaponPresentationCamera="player-main-camera-only-v1";};'
          new='renderHook=function(...args){previousSceneOnBeforeRender?.apply(this,args);const mainCamera=bridge()?.threeCamera;if(!mainCamera)return;const gun=scene.getObjectByName?.("WALK_PISTOL_3D");if(!gun)return;const mode=String(footWeapons()?.mode||"pistol");setWeaponModeVisual(gun,mode);applyLatchedAimTransform(gun,mainCamera,performance.now());const view=viewport();if(view)view.dataset.walkWeaponPresentationCamera="player-main-camera-authoritative-v2";};'
          assert old in s, 'main-camera guard contract changed'
          p.write_text(s.replace(old,new,1))
          PY
          node --check sim/first_person_weapon_runtime_v3.mjs
          grep -q 'player-main-camera-authoritative-v2' sim/first_person_weapon_runtime_v3.mjs
          ! grep -q 'camera!==mainCamera' sim/first_person_weapon_runtime_v3.mjs
      - name: Commit repair
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add sim/first_person_weapon_runtime_v3.mjs
          git diff --cached --quiet && exit 0
          git commit -m 'Use player camera for FPS weapon presentation'
          git push origin HEAD:main
