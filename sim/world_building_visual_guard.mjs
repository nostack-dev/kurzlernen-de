import {syncWorldBuildingDepthOcclusion} from "./world_building_depth_occlusion.mjs";

const FALLBACK_BUILDING_COLOR="#d8d2c8";
function isBuildingLayer(layer){const id=String(layer?.id||"").toLowerCase(),sourceLayer=String(layer?.["source-layer"]||"").toLowerCase();return layer?.type==="fill-extrusion"&&(id.includes("building")||sourceLayer.includes("building"));}
function opaqueColor(value){if(Array.isArray(value))return value;if(typeof value!=="string")return FALLBACK_BUILDING_COLOR;const rgba=value.match(/^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)(?:\s*,\s*[\d.]+)?\s*\)$/i);if(rgba)return`rgb(${rgba[1]},${rgba[2]},${rgba[3]})`;if(/^#[0-9a-f]{6}$/i.test(value)||/^#[0-9a-f]{3}$/i.test(value)||/^rgb\(/i.test(value))return value;return FALLBACK_BUILDING_COLOR;}
export function enforceOpaqueBuildingLayers(map){let changed=0;if(map?.getStyle&&map?.setPaintProperty){for(const layer of map.getStyle()?.layers||[]){if(!isBuildingLayer(layer))continue;try{map.setPaintProperty(layer.id,"fill-extrusion-opacity",1);const color=map.getPaintProperty?.(layer.id,"fill-extrusion-color"),opaque=opaqueColor(color);if(opaque!==color)map.setPaintProperty(layer.id,"fill-extrusion-color",opaque);changed++;}catch{}}}syncWorldBuildingDepthOcclusion();return changed;}
